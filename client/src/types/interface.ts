// eslint-disable-next-line @typescript-eslint/no-unused-vars
interface Admin {
  id: number;
  username: string;
  password: string;
}

interface Issue { 
  id : number,
  name : string
}

interface Symptom {
  id : number,
  name : string
}